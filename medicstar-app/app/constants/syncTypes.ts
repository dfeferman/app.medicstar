export enum ActionType {
  TOGGLE_AUTO_SYNC = "toggle-auto-sync",
  FORCE_SYNC = "force-sync",
  STOP_PENDING_TASKS = "stop-pending-tasks",
}

export enum SyncType {
  PRODUCT = "product",
  TRACKING = "tracking",
}
